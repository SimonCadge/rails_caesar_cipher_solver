<!DOCTYPE html>
<html>
  <head>
    <title>CipherWebapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag asset_path('key-fill.svg') %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex tertiary">
    <div class="container min-vh-100 d-flex flex-column p-0 primary">
      <nav class="navbar navbar-expand-sm navbar-light p-2 m-2 rounded secondary">
        <a class="navbar-brand" href="#">
          <%= image_tag("key-fill.svg", width: "30", height: "30")%>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Solver", "/solver", class: current_page?("/solver") ? "nav-link active" : "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Info", "/info", class: current_page?("/info") ? "nav-link active" : "nav-link" %>
            </li>
          </ul>
        </div>
      </nav>
      <div class="d-flex flex-column flex-grow-1 p-2">
        <%= yield %>
      </div>
      <div class="flex-column p-3 secondary">
          <%= render "solver/banner" %>
      </div>
    </div>
  </body>
</html>
